name: Deploy to Windows Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.WINSOWSSERVER_SSH_PRIVATE_KEY }}
  
      - name: SSH to Windows Server
        run: |
            ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null kassab@81.17.102.241 'cd C:\\nginx\\nginx-1.26.0\\apps\\riskman-client && ./build_script.sh'
      

      # - name: Verify SSH Access
      #   run: ssh -o StrictHostKeyChecking=no mkarrowtradefx@81.17.102.241 'echo "SSH connection successful"'
    
      # - name: SSH to Windows Server and trigger build script
      #   run: ssh -o StrictHostKeyChecking=no mkarrowtradefx@81.17.102.241 'cd C:\nginx\nginx-1.26.0\apps\riskman-client && ./build_script.sh'
        # continue-on-error: true  # This will allow the job to proceed even if the SSH command fails, which can be useful for debugging
